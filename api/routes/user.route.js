import express from 'express';
// You can import other user-related functions here if necessary

const userRoutes = express.Router();

// Add other user-related routes, but do not duplicate signup

export default userRoutes;  // Correct ES Module export
